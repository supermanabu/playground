a = np.array([1, 2, 3, 4, 5])
b = np.array([5, 4, 3, 2, 1])
c = a + b
# Inner product
inner_product = np.inner(a, b)

# Cross product
cross_product = np.cross(a, b)

# Module
mod = np.linalg.norm(a)

# Matrix transpose
tp = np.transpose(a)

# Matrix multiplication
product = np.matmul(a, b)

# integral
integral = np.trapz(y, x)
